{
  "name": "Smithery (hookfish workspace)",
  "install": "corepack enable && pnpm install --frozen-lockfile",
  "terminals": [
    {
      "name": "dev",
      "command": "pnpm dev",
      "description": "Runs the app watcher (@hookfish/app) and the TanStack Start node example on http://localhost:3000 with PGlite (no external Postgres required)."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
